# Chart metadata used by Helm when packaging and installing the gateway.
apiVersion: v2
name: api-gateway
description: Helm chart for the OCR FastAPI gateway
version: 0.1.0
appVersion: "1.0.0"
type: application
